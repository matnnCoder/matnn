var greeting = "Hello";
var name = "Matnn";
var message = greeting + ", " + name + "!";

Console.log("字符串拼接:", message);
Console.log("字符串长度:", message.length);

var str = "abcdef";
Console.log("字符串:", str);
